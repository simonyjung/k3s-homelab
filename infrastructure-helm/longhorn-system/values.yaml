---
longhorn:
  # Needed when installind with argoCD
  preUpgradeChecker:
    jobEnabled: false
  longhornUI:
    replicas: 1
  # defaultSettings:
  #   backupTarget: "nfs://x.x.x.x:/mnt/user/backups"
