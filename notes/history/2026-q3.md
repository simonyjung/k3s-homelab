# Completed items — 2026 Q3

Archive of finished items from [`../plan.md`](../plan.md), one file per quarter.
This file covers the 2026-07-01/02 review rounds and their follow-ups.

## 1. Broken — fix first

| # | Status | Item |
|---|--------|------|
| 1.1 | `done` | **Fix traefik values schema break.** Rewrote `infrastructure-helm/traefik/values.yaml`: `entryPoints:` → chart-native `ports.web.http.redirections.entryPoint` + `ports.websecure.http.tls.enabled` (v40 nests both under `http`). Validated with `helm template` (33 resources, redirect + TLS args, Certificate, dashboard IngressRoute all render). Bonus finding: the old `entryPoints:` block was silently ignored by pre-v40 charts, so the HTTP→HTTPS redirect had never been active. Deployed in `701bb22`: app is back to Synced/Healthy, redirect verified in-cluster (`301 -> https://...`). Renovate PR #63 (traefik v41) now unblocked. |
| 1.2 | `done` | **Fix `monitoring` OutOfSync.** Added `ServerSideApply=true` to `syncOptions` in `appsets/infrastructure-helm-applicationset.yaml` (`bb20954`) — CRDs were too large for the client-side last-applied annotation. Monitoring is Synced/Healthy; all 16 apps green. Unblocks kube-prometheus-stack v87 (PR #66). |

## 2. GitOps gaps — running in cluster, absent from git

| # | Status | Item |
|---|--------|------|
| 2.1 | `done` | **Bring 1Password operator + Connect into git.** `infrastructure-helm/1password/` umbrella chart (connect 2.4.1, `fc2da1b`) deployed into the `1password` namespace; credentials copied there manually. Gotcha: chart mounts credentials as a *file* (raw JSON) while the old env-var install used base64 — secret had to be re-created decoded, or Connect 500s with "failed to Unmarshal credentials". Old hand-install in `default` torn down 2026-07-02; verified operator reconciling, all 14 apps Healthy after. Old secrets in `default` deleted 2026-07-02 — sole in-cluster copy of the credentials now lives in the `1password` namespace. |
| 2.2 | `done` | **Self-manage ArgoCD.** Turned out to already be helm-installed (argo-cd 8.5.10, release `argocd`, default values) — mirrored into `infrastructure-helm/argocd/` (`6ff766c`); pre-flight `kubectl diff` showed zero drift, adoption completed with no pod restarts. Preserved the out-of-band NodePort UI service (31362/31910) in values. Bonus cleanup: uninstalled unused argocd-image-updater (+ its CRD) and a 219d-orphaned Bitnami redis in the argocd ns (from a deleted `redis` app; freed 4×8Gi PVCs). Renovate now manages argo-cd chart bumps via Chart.yaml. Leftover: stale `sh.helm.release.v1.argocd.v1` secret in argocd ns can be deleted. |
| 2.3 | `done` | **Removed external-secrets operator** 2026-07-02 (`helm uninstall` + namespace). Verified before removal: zero instances across all 23 ESO CRDs, no dependency on the 1Password stack (separate API groups, webhook scoped to ESO kinds only). CRDs and webhook gone; all apps/pods healthy after. If ever migrating off the 1Password operator to ESO's 1Password provider, reinstall via GitOps. |

## 3. Cleanup

| # | Status | Item |
|---|--------|------|
| 3.1 | `done` | Delete dead `apps/cloudflared/` directory. Removed in `3268225`; PR #64 rebase requested so it only touches live per-app cloudflared.yaml files. |
| 3.2 | `done` | Deleted empty leftover namespaces 2026-07-02: `cloudflared-production`, `grafana`, `httpbin-development`, `kube-prometheus-stack` (stale secrets in the latter two were superseded by live equivalents in `monitoring`). `1password` was on the original list but is now in active use (2.1) and stays. |
| 3.3 | `done` | Fix `montioring`/`grafrana` typos — **turned out to be a live TLS outage**: the certificate template also set `secretName` from `certificate.name` instead of `certificate.secretName`, so renewals went into the wrong secret and grafana served an expired cert for ~5 months. Fixed in `b7aea2e`; grafana now serves a valid cert (expires 2026-09-30, verified in-cluster); orphaned typo secret deleted. Other charts checked — bug was monitoring-only. |
| 3.4 | `skipped` | ~~Trim openwebui values file~~ — moot: open-webui app removed entirely (`b2429e3`), along with ollama (`86569d7`). Local AI experiment retired; PRs #68/#69 closed. |
| 3.5 | `done` | `infrastructure-applicationset` kept and now in use: `infrastructure/system-upgrade/` (`78e9bac`) is its first app. Bonus bug found: the appset's glob `infrastructure/*/` (trailing slash) never matched anything — ArgoCD's per-segment matching can't match it; fixed to `infrastructure/*` in `54a129a`. |
| 3.6 | `done` | Fixed `docs/setup.md` GPU node install command (`1cdbcdf`): `--node-label "gpu=amd"` now correctly placed after `sh -s -` (was invalid bash with the wrong label `gpu-amd`). README also gained a "Kubernetes updates" section documenting the Renovate → plans.yaml → SUC upgrade flow. |
| 3.7 | `done` | Migrated `patchesStrategicMerge` → `patches` (`1cb247d`) in the four overlays that used it (linkding dev, transmission/django-starter/homepage prod — plan originally misattributed plex). `kustomize edit fix` + reformat to repo style; rendered output verified byte-identical for all four. |

## 4. CI & dependency process

| # | Status | Item |
|---|--------|------|
| 4.1 | `done` | **Run CI on pull requests.** Added `pull_request` trigger to `ci.yaml` (`36b1150`). Renovate PRs now get lint + render checks before merge. Note: pushing workflow files required `gh auth refresh -s workflow`. |
| 4.2 | `done` | **Add render validation to CI.** New `validate` job (`36b1150`): kustomize-builds all `apps/*/envs/*` overlays and helm dependency-build + templates every umbrella chart against each values file (kustomize/helm are preinstalled on ubuntu-latest). Verified green on run 28558655240. Would have caught the traefik v40 break pre-merge. Follow-up idea: kubeconform. |
| 4.3 | `done` | **Renovate backlog cleared — 0 open PRs.** Merged & verified live: #63 traefik v41, #66 kube-prometheus-stack v87, #70 transmission 4.1.3, #67 cert-manager v1.20.3, #62 ollama 0.31.1, #65 checkout v7, #64 cloudflared 2026.6.1 (homepage + linkding tunnels rolled out). Closed without merging: #61 redis, #68/#69 open-webui (apps deleted). |
| 4.4 | `done` | **Redis decision: deleted.** `apps-helm/redis` removed in `3268225` (no consumers in repo or cluster; avoids Bitnami image lockdown). PR #61 closed. ArgoCD cascade-deleted the workloads; PVCs + `redis-development` namespace removed manually. |
| 4.5 | `done` | Install `yamllint` locally (`brew install yamllint`) so the CI gate can run before push. Installed 2026-07-01 along with helm. |

## 5. Nice-to-have

| # | Status | Item |
|---|--------|------|
| 5.1 | `done` | **Longhorn recurring jobs deployed** (`590b8b8`) — discovery: the NAS backup target had NEVER been used (zero backups existed). Now: backup-daily 02:00 retain 7, backup-weekly Sun 03:00 retain 4, snapshot-cleanup 04:00 (112 system snapshots had accumulated), filesystem-trim (replaces unmanaged UI job, deleted). All default-group → cover every volume. Backups are block-incremental and retain-bounded, so NAS usage ≈ one full copy + retained churn per volume; manual backups are never auto-pruned. NAS path validated end-to-end with a one-off backup (Completed, 128MiB) then cleaned up. First real backups land at 02:00. Note (2026-07-03): schedules run in **America/New_York** (k3s host TZ), not UTC. |
| 5.2 | `done` | Probes added (`61e8b13`): readiness on all four apps (linkding `/health`, homepage `/api/healthcheck` + Host header — bare probe gets 400 from ALLOWED_HOSTS; plex `/identity`; transmission tcpSocket 9091 — web UI 401s behind basic auth). Liveness only on plex + transmission with ~2min tolerance; plex startupProbe 5min. All endpoints verified in-cluster before deploying; all apps Synced/Healthy after rollout. |
| 5.3 | `done` | **Move plex to the GPU host (`amleyg00`).** Deployed in `26a748a`: nodeSelector → amleyg00, `/dev/dri` mounted (privileged, ollama pattern). Verified: pod Running on amleyg00, `card1`+`renderD128` visible in container, `/identity` returns 200, app Synced/Healthy. Remote access fixed 2026-07-01: root cause was double NAT at the ISP gateway, resolved upstream of the cluster (Cloudflare-tunneled services never noticed, being outbound-only); verified working from external vantage points. Still to verify: "(hw)" on a transcode in the Plex dashboard. |
| 5.4 | `done` | **Reduced amleyg00 BIOS iGPU VRAM carve-out** (2026-07-03). Was 24GiB of 32GiB (ollama-era), leaving the OS 7.5GiB at ~57% used. Set to minimum in Framework BIOS (F2 → Advanced → iGPU memory allocation); allocatable jumped 7.5Gi → 29.2Gi. Procedure: cordon+drain (Longhorn instance-manager PDB retries expected), poweroff, BIOS, uncordon — monitoring stack failed over cleanly, plex Pending during (gpu=amd pin), volumes degraded→resynced. Gotcha: first attempt didn't take (setting not saved) — verify with `free` on the node, not just node Ready. Follow-up in same PR: plex memory limit 2Gi → 4Gi at the marker in `apps/plex/base/deployment.yaml`. |
| 5.5 | `done` | **Upgraded k3s 1.33.3 → 1.36.2 (current stable)** on 2026-07-02 via system-upgrade-controller (`infrastructure/system-upgrade/`, GitOps Plans, cordon-only, server-first). Three hops, one commit each: v1.34.9 → v1.35.6 → v1.36.2; all nodes/apps/volumes verified healthy after each. Longhorn 1.12 lists k8s 1.36 as tested. Note: 1.33 hit upstream EOL 2026-06-28, so this cleared an EOL version. Fallout fixed along the way: k8s 1.34's STS `persistentVolumeClaimRetentionPolicy` defaulting exposed that ServerSideApply needs ServerSideDiff (`4ed7bb6`) — grafana STS was stuck OutOfSync under the legacy differ. Future upgrades = bump `spec.version` in `plans.yaml`. |
| 5.6 | `done` | **ArgoCD upgraded 3.1.8 → 3.4.4 and UI moved to a domain** (2026-07-02). Chart ladder 8.6.4 (#72) → 9.3.5 → 9.5.11 → 10.1.0 (superseded #73, auto-closed), each hop self-applied and verified; `global.networkPolicy.create` pinned false pending review. UI now at `https://argocd.cluster.simonjung.io` (chart-native Certificate + traefik Ingress, `server.insecure` behind TLS termination); NodePort retired; homepage tile updated with siteMonitor. Last redis-orphan leftover (netpol in argocd ns) deleted. |

## 6. Round-2 review (2026-07-02, post-overhaul)

| # | Status | Item |
|---|--------|------|
| 6.1 | `done` | **Alerts now reach a human — Pushover** (PR #76). Route: warning/critical → Pushover via `*_file` creds from `pushover-alertmanager` OnePasswordItem; Watchdog + info nulled; stock inhibitions kept. Verified: secret materialized, config loaded (amtool), synthetic test alert delivered. Follow-up PR #77 disables the three eternal k3s false positives (KubeScheduler/ControllerManager/Proxy Down + etcd scraping — k3s embeds these/uses sqlite) that would page every 12h. Follow-up PR #82: allowlist routing (default → null, only `severity =~ warning|critical` pages) after InfoInhibitor (severity "none") flapped through the blocklist. |
| 6.2 | `done` | **cert-manager metrics + expiry alerts** (PR #78). ServiceMonitor enabled; PrometheusRule: NotReady 30m (crit), <21d (warn), <7d (crit) — calibrated vs cert-manager's ~30d renewal so any fire = renewal genuinely broken. Prometheus selectors opened (`*SelectorNilUsesHelmValues: false`) for cross-release discovery. Verified live: 7 certs scraped (57–90 days out, none near threshold), all 3 rules loaded. The grafana-incident failure class now pages. Note: prometheus container is distroless — use `promtool`, not `kubectl exec sh`. |
| 6.3 | `done` | **LB SPOF fixed — MetalLB L2 mode** (PR #83). Pool `10.0.10.200-210` (in LAN subnet, outside DHCP), VIPs pinned: traefik `.200`, linkding-dev `.201`. Wildcard DNS → `10.0.10.200`; klipper servicelb disabled on amley00 (`disable: [servicelb]` in `/etc/rancher/k3s/config.yaml` — reapply if server rebuilt; documented in docs/load-balancing.md + setup.md). Verified: svclb daemonsets gone, VIP serves via DNS end-to-end, old node-IP path closed, public tunnels unaffected, 16 apps green. BGP mode considered, rejected (out-of-git FRR file on UCG). Gotcha hit: MetalLB controller allocator went stale during pool transition (contradictory AllocationFailed) — controller restart rebuilds state. Failover drill passed 2026-07-03 during node upgrades: two live failovers (amley01→amley02→amley01), zero dropped probes. Follow-up PR #85: traefik replicas=2 after a 1-replica stranding caused ~5 min ingress outage. |
| 6.4 | `done` | **Node OS hygiene — fleet on F44 stable.** Agents (2026-07-03): amley01/02/amleyg00 upgraded F43→F44 one-at-a-time; dnf5-automatic enabled (security-only, reboot=when-needed), staggered Tue/Wed/Thu 03:00 America/New_York; docs/node-updates.md (PR #84). amley00 (2026-07-04): the frozen Rawhide tracker fixed by **clean F44 reinstall** — post-HA (6.10) no k3s state preservation needed: drain + `kubectl delete node`, wipe, provision per docs/setup.md, rejoin as server pointing at amley01 with pinned `INSTALL_K3S_VERSION=v1.36.2+k3s1`; etcd re-replicated, Longhorn stayed 7/7 healthy, all apps green. Gotchas hit: k3s.service fails at start if `token:` is missing from config.yaml; the node registered `SchedulingDisabled` (uncordon manually); fresh installs default to UTC (set America/New_York before the timer); the bare install script would have installed a newer k3s than the cluster (hence the version pin). dnf5-automatic enabled on the reserved Monday 03:00 slot — first run 2026-07-06. Docs from the exercise: PR #99 (why swap must be off), PR #100 (step-by-step dnf5-automatic + rawhide-fix rewrite). Related incident logged at 6.3: single-replica traefik stranding → PR #85 (replicas=2). |
| 6.5 | `done` | **CI validates `infrastructure/*`** (PR #79) — system-upgrade dir now render-checked; all 8 kustomize dirs verified locally. |
| 6.6 | `done` | Docs refreshed (PR #79): README tree matches reality (deleted apps out; infrastructure/, 1password, argocd, metallb, traefik in; docs links). CLAUDE.md stale notes fixed directly (gitignored). |
| 6.7 | `done` | **Basic-auth on Longhorn UI + Traefik dashboard** (PR #80). Traefik basicAuth Middleware in both namespaces backed by the shared `dashboard-basic-auth` OnePasswordItem (htpasswd `users` field). Verified live: both hosts 401 unauthenticated (were open); secrets materialized with correct key. User confirms 200-with-credentials via browser login. Docs: docs/dashboard-auth.md (PR #81). |
| 6.8 | `done` | **Renovate watches vendored SUC images** (PR #86). Docker regex manager extended to system-upgrade-controller.yaml incl. a matchString for the uppercase `SYSTEM_UPGRADE_JOB_KUBECTL_IMAGE` env var; controller bumps get a re-vendor warning via prBodyNotes (tag bump ≠ re-vendored RBAC/CRDs; docs/upgrades.md updated). Verified end-to-end: Renovate immediately opened PR #87 bumping rancher/kubectl v1.30.3→v1.36.2 (was 6 minors outside kubectl skew vs the v1.36 apiserver). |
| 6.10 | `done` | 1Password vault confirmed to hold copies of `1password-credentials.json` + operator token (user verified 2026-07-02) — cluster is no longer the only holder. |
| 6.11 | `done` | *(numbered 6.10 in plan.md)* **HA control plane conversion — COMPLETE 2026-07-12** (converted 2026-07-03, stability watch closed 07-12). SQLite backed up off-node (integrity-checked); amley00 migrated to embedded etcd (`cluster-init: true`); amley01+02 drained and rejoined as servers (`disable: [servicelb, traefik]`), Longhorn rebuilt between nodes. End-to-end verified: 3 etcd members healthy, API answers on all three server IPs, apps green. kube-vip ARP VIP `10.0.10.250` live (PR #89), kubeconfig switched; docs PR #88. Scheduled etcd snapshots verified from day one (00:00/12:00 per server, retention 5, `kubectl get etcdsnapshotfiles`). **Stability watch 07-03→07-12**: no leader-election churn or apiserver instability through 9 days including an amley00 F44 reinstall (6.4), staggered dnf5-automatic reboots, and the 07-11 rolling k3s restarts (8.5b local-storage disable); snapshots on schedule throughout (~94MB → 43MB after natural compaction). Etcd-fsync-vs-Longhorn-IO concern never materialized; k3s journal searchable in OpenObserve for future spot checks. Follow-up lives in plan.md deferred: offsite snapshot copy. |

## 7. Remote access

| # | Status | Item |
|---|--------|------|
| 7.1 | `done` | **Tailscale remote access (2026-07-03, PR #95)** — official Tailscale K8s operator 1.98.4 as `infrastructure-helm/tailscale/` umbrella chart: subnet router (Connector CR) advertising `10.0.10.0/24` + API-server proxy in auth mode for remote kubectl. OAuth creds via 1Password (`operator-oauth` Secret mounted through `oauthSecretVolume`; item fields must be labeled `client_id`/`client_secret`). Out-of-git prereqs (documented in chart README): tailnet ACL tagOwners `tag:k8s-operator`/`tag:k8s`, autoApprovers for the route (worked — no manual approval needed), impersonation grant (`autogroup:admin` → `system:masters`), OAuth client (Devices Core + Auth Keys write). Verified end-to-end: app Synced/Healthy, operator + connector Running, route exposed, **off-LAN test from cellular passed** (LAN + kubectl reachable). Gotchas: helm templates must use inline-only Go templating (`toYaml \| nindent`, not `range` blocks) to stay yamllint-parseable; ArgoCD briefly showed Degraded from a stale health snapshot taken seconds before the connector went Ready — a refresh cleared it. Client note: Linux needs `tailscale up --accept-routes`; `tailscale configure kubeconfig tailscale-operator` for the API-proxy path. |

## 8. HA audit (2026-07-03)

| # | Status | Item |
|---|--------|------|
| 8.1 | `done` | **HA: failover hardening (zero cost)** — PR #102, merged + verified live 2026-07-04. (a) longhorn `nodeDownPodDeletionPolicy: delete-both-statefulset-and-deployment-pod` — Setting CR confirmed flipped (the feared silent-ignore of `defaultSettings` changes didn't happen); pods on a dead node now force-delete so RWO volumes reattach. (b) traefik: hostname TSC (DoNotSchedule) + PDB minAvailable 1 + `system-cluster-critical` + requests 50m/64Mi — verified pods on distinct nodes, priority class admitted outside kube-system, PDB live with 1 allowed disruption. (c) same TSC in homepage+linkding cloudflared — both pairs spread across nodes. (d) transmission `strategy: Recreate` (closes the RWX-config two-daemons race on every Renovate bump) + 60s not-ready/unreachable tolerations. (e) linkding livenessProbe (`/health:9090`, 30s×4) + 60s tolerations — a wedged gunicorn now restarts. Notes: linkding already had `Recreate` so (d) was transmission-only; traefik chart picks PDB apiVersion from cluster capabilities (offline `helm template` shows v1beta1 — harmless); rollout gotcha: ArgoCD polled the merge commit into only 2 of 5 apps at first — a `argocd.argoproj.io/refresh` annotation syncs the stragglers instead of waiting out the poll interval. |
| 8.5 | `done` | **HA audit: subtraction/cleanup — COMPLETE 2026-07-12.** (a) argocd dead weight removed (PR #196): `dex.enabled: false` (no SSO; local admin only — deliberate tension with the deferred ArgoCD-SSO item, Dex stays off unless SSO is actually wired) + `notifications.enabled: false` (zero triggers; alerting runs through Prometheus/Alertmanager) — ~45Mi and two pods freed; self-managed argocd app pruned both cleanly, remaining components did one expected roll (shared `cmd-params` checksum), UI verified 200. (b) latent StorageClass bug fixed 2026-07-11 (docs PR #195): BOTH `local-path` and `longhorn` were annotated default — orphaned 329-day-old `default/data-my-release-mariadb-0` local-path PVC deleted first (reclaim needs the provisioner alive; on-disk data on amley02 confirmed reclaimed), then `local-storage` added to `disable:` on all three servers + rolling restarts. Verified: `local-path` SC + provisioner gone, longhorn sole default. **Startup-race note**: during the last server's restart the deploy controller transiently re-applied the local-storage AddOn (Deployment existed ~30s, configmap already gone, then deleted) — benign; all three configs re-verified 2026-07-12. Skip-list from the audit (verified, don't revisit): argocd application-controller (replicas=sharding not redundancy w/ 1 cluster), grafana/linkding/plex (sqlite, 8.1 failover covers), transmission (single daemon), metallb/kube-vip (already HA), metrics-server/KSM/prometheus-operator/1password-operator/SUC (reconcile-pause only). |

## 9. Platform capabilities

| # | Status | Item |
|---|--------|------|
| 9.1 | `done` | **Application benchmarking (k6 → Prometheus → Grafana)** — built and verified end-to-end 2026-07-04. Infra (PR #104): `infrastructure-helm/k6-operator/` umbrella chart (grafana chart 4.5.0, TestRun CRD), `enableRemoteWriteReceiver: true` on prometheusSpec, official k6 dashboard (gnetId 19665, trend-stats variant) auto-provisioned into Grafana's Benchmarks folder. Scenarios (PR #105): `benchmarks/` at repo root, **deliberately outside all appset globs** — TestRuns are imperative (`kubectl apply -k benchmarks/<scenario>/`), never ArgoCD-synced; runners pinned to the `gpu: amd` node. Run log: `benchmarks/results.yaml` (PR #106) — append per run, schema snapshots the target's image/replicas/limits at run time; **Prometheus retention is 10d, git is the durable record**. Proved its worth immediately: run 1 found homepage's 100m CPU limit capping it at ~30 RPS with 18% failures and p95 up to 37.8s; after 8.2(c) (2×500m) the identical profile did the full 100 RPS with 0 errors and p95 22.6ms. Gotchas: k6 exports durations in **seconds**; the dashboard's duration panels need the "Trend Metrics Query" variable set to the `k6_http_req_duration_*` stats; `peak_rps_instant` panels overread vs the 1m sustained rate. New scenarios: copy `homepage-ramp/`, new testid, log to results.yaml (write-path only against `-development`). |
| 9.2 | `done` | *(numbered 9.1 in plan.md — renumbered here, 9.1 above was already archived)* **linkding production → CNPG postgres — COMPLETE 2026-07-05.** Dev experiment stack applied (results.yaml records 9-14: CNPG cluster + pgbouncer Pooler + persistent-connections custom.py shim; the 31→1249 rps arc). Phase A PR #142 (cluster 2/2 on longhorn-1replica, pooler); phase B PR #143 (cutover). Migration verified: dumpdata from PVC sqlite inside the new pod (`LD_DB_ENGINE=sqlite` — the engine guard exists for exactly this) → loaddata → counts match baseline (1 user/1 bookmark/1 tag), password hash usable, `CONN_MAX_AGE None`/`sslmode disable` live, LAN ingress + public tunnel both 200. Sessions survived (secretkey.txt on the preserved PVC). Rollback: revert #143 — sqlite file untouched on the PVC. Follow-ups if ever needed: failover drill on the prod cluster (dev drill: record 9, ~3m20s pod-delete, faster via `cnpg promote`); the 1-replica decision + scale-out recipe live in plan.md's follow-ups section. |
| 9.3 | `done` | *(numbered 11.1 in plan.md)* **OpenObserve log storage — COMPLETE 2026-07-05** (PRs #163/#164 chart — #164 carried the commit after a branch collision; #165 homepage card; #166/#167/#168 fixes). New `infrastructure-helm/openobserve/` umbrella: openobserve-standalone 0.80.3 (20Gi `longhorn-1replica` PVC — logs disposable, no 3x replication; 15d retention; telemetry+GeoIP off; preferred affinity amleyg00, keeps ingest IO off the etcd servers) + fluent-bit 0.57.8 DaemonSet → streams `containers` (pod logs, k8s-metadata-enriched, host cursor DBs so restarts don't re-ingest) and `k3s` (k3s/k3s-agent journal incl. embedded etcd — replaces manual `journalctl` watching from 6.10). UI logs.cluster.simonjung.io (LAN-only); root creds via `auth.existingRootUserSecret` from 1Password login item `openobserve-root-credentials` (username must be an email). **Three post-deploy fixes, all verified live**: (a) amleyg00 exhausts `fs.inotify.max_user_instances` (Fedora default 128) → tail crashloops with errno=24; `Inotify_Watcher false` = stat-based polling, no node sysctl (PR #166). (b) The pipeline was the cluster's top log producer — fluent-bit logged every delivery at info (41/50 sampled records were the pipeline itself) → `logLevel: warn`, plus `Annotations Off` and duplicate `time` field dropped (PR #167). (c) The server narrated every search at info (~120 lines/s with a dashboard open: flight/grpc/wal/storage/cache modules + access log per ingest POST) → `RUST_LOG: warn` (PR #168). Result: openobserve-0 14,125 → 22 lines per 2 min, collectors 0. "Kubernetes Logs" dashboard (7 panels: volume/errors by namespace, top pods, by-node, k3s journal by host incl. priority≤4 warnings, recent-errors table) imported via API — lives in the o2 store, not git. Remaining follow-ups → plan.md deferred section (backup-group exclusion, ingestion user, retention review, dashboard JSON durability). |

## 10. CI guardrails & policy (2026-07-05)

| # | Status | Item |
|---|--------|------|
| 10.2 | `done` | **CI schema validation (2026-07-05)** — the `validate` job now pipes every render through kubeconform v0.8.0 instead of only checking "does it render": kustomize overlays + `root-argocd-app.yaml`/`appsets/` validate **strict with no escape hatches** (datree CRDs-catalog covers all 5 hand-written CRD kinds — onepassword/cert-manager/cnpg/argoproj/upgrade.cattle.io — all schema URLs verified 200); helm renders add `-ignore-missing-schemas` (upstream CRs). `-kubernetes-version` is grep'd from `system-upgrade/plans.yaml` at run time, so validation tracks K3s upgrades automatically (same trick as the README k3s badge). `-skip CustomResourceDefinition` everywhere (the CRD *kind* schema is 404 in yannh/kubernetes-json-schema). `set -o pipefail` added — without it a failed `kustomize build` piped to kubeconform would pass silently (kubeconform exits 0 on empty stdin). Verified locally pre-PR: ~450 rendered resources across all overlays/charts, 0 invalid. Gotcha for local runs: helm 4 dropped auto-fetch of "unmanaged" repos, so `helm dependency build` needs `helm repo add` first (runner helm 3 unaffected); stale gitignored `Chart.lock` from old local renders also breaks dep build — `helm dependency update` fixes. |
| 10.3 | `done` | **Render-diff PR previews (2026-07-05)** — new `render-diff` workflow (pull_request only): renders every kustomize overlay + helm chart from both the PR merge commit and current `main` via shared `scripts/render-all.sh` (23 files), diffs, and upserts a single marker-keyed PR comment (`<!-- render-diff -->`, PATCH not re-post) with a per-app `+/−` table + collapsed full diff (truncated at 55KB, GitHub comment cap 64KB). The per-app table catches the `prune: true` foot-gun: an app dir that stops matching a generator glob shows as `+0 / −N` (verified in local simulation). Renders confirmed **byte-deterministic** (two consecutive full renders diff clean — no chart uses randAlphaNum et al.), so diffs are signal-only. Both trees render with the PR's copy of the script; base uses `pull_request.base.ref` (current main, matching the merge-commit semantics). Renovate PRs get diffs for free (same-repo branches; fork PRs would lack comment perms — none occur here). Tier 2 (live `argocd app diff` incl. ApplicationSet evaluation — needs a tailscale ephemeral node or self-hosted runner) deliberately deferred. |

## 12. Security audit (2026-07-06)

| # | Status | Item |
|---|--------|------|
| 12.4 | `done` | **Security: NetworkPolicies (flat pod network)** — zero NetworkPolicies cluster-wide (verified live); any pod — including internet-facing homepage/plex/transmission — can reach the 1Password Connect API (fronts the entire vault), the CNPG postgres clusters, glitchtip's valkey, **Prometheus's unauthenticated remote-write receiver** (`enableRemoteWriteReceiver: true` in monitoring values — any pod can poison metrics and the alerts/badges built on them), OpenObserve ingest, and the k8s API. K3s enforces netpol natively (the argocd values comment confirms). Incremental rollout, highest value first: (1) ~~`1password` ns: default-deny ingress, Connect reachable only from the operator~~ **PR #206** (2026-07-12): default-deny-ingress + allow operator→connect:8080 as umbrella templates (8081/sync deliberately excluded — api/sync talk over pod-local localhost, invisible to netpol; nothing scrapes Connect, verified no matching Service/PodMonitor). **DONE + verified live 2026-07-12**: app Synced, Connect pod stays Ready — confirms kubelet probes survive default-deny under kube-router (safe to template the pattern), OnePasswordItem reconcile works through the allow (operator log clean), scratch pod in default ns gets connection-refused to `onepassword-connect.1password:8080` (kube-router REJECTs rather than drops); (2) ~~`glitchtip-production`: postgres/valkey only from glitchtip pods + CNPG operator~~ **PR #207, DONE + verified live 2026-07-12**: default-deny + same-namespace allow (namespace-wide on purpose — migrate Job pods carry only job-name labels) + CNPG operator :8000 + prometheus :9187; web :8000 stays open cluster-wide BY DESIGN (traefik/prometheus/sentry-sdk reporters from any app ns — enumerating reporter namespaces = a netpol PR per new DSN; the datastores are the boundary). Verified: UI 200 via traefik, both scrape targets up, CNPG 2/2 healthy, migrate hook re-ran Completed, scratch pod cross-ns: 5432 BLOCKED / 6379 BLOCKED / web:80 reachable. **Follow-up PR #209**: cloudbeaver (LAN-only DB admin UI) regained postgres 5432 as a deliberate standing allowance (pod-label + port scoped; acknowledged lateral-movement bridge — revisit if cloudbeaver ever goes off-LAN); adopt as part of the db-netpol pattern when the rollout reaches linkding/dinglink; (3) `monitoring`: prometheus ingress from grafana/kromgo/homepage only, remote-write from the k6/benchmarks namespace only — **PR #208** (2026-07-12): surgical single policy selecting ONLY the prometheus pods (no namespace-wide deny: the operator admission webhook is API-server-called from node IPs = cert-manager-webhook failure class, and grafana/alertmanager ingresses stay untouched); same-ns all ports (grafana + self-scrape of reloader :8080), kromgo/homepage/benchmarks → 9090 only. **DONE + verified live 2026-07-12**: kromgo badges serve live values, homepage logs clean, all 69 scrape targets up / 0 down, scratch pod in default ns blocked from 9090; k6 remote-write still to confirm on the next benchmark run. **Gotcha learned during #209 verify**: kube-router takes a few seconds to add a NEW pod's IP to policy source ipsets — a fresh scratch pod's first connection attempt can be spuriously BLOCKED (and any just-started pod in an allowed ns has the same brief window); netpol tests need a retry loop; (4) ~~flip `networkPolicy.create: true` in argocd values after reading the chart's policies~~ **PR #210** (2026-07-12): reviewed the 4 rendered chart policies — redis + repo-server locked to the argocd components that use them (redis was reachable cluster-wide before), controller = metrics-only from any ns, server = allow-all (UI behind traefik, effectively unrestricted); dex disabled, applicationset/notifications pods unselected = stay open, selectors match pods by construction (same release). **DONE + verified live 2026-07-12**: argocd app self-synced Healthy at the merge revision, 4 netpols live (selectors carry the real `instance=argocd` release labels), UI 200 via traefik, post-policy hard refresh of another app completed Synced/Healthy — server→repo-server→redis all exercised with the policies active, scratch pod in default ns blocked from argocd-redis:6379 across 4 retries (no ipset-race false negative). Egress policies can wait (tracked as a 12.4 leftover). Remaining after (4): db-netpol pattern for linkding/dinglink (+ cloudbeaver allowance), OpenObserve ingest, namespace-wide monitoring tightening — all lower value, fold into 12.6-era sweeps. |
