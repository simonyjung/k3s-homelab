# Cluster & Repo Improvement Plan

Active items only — completed work is archived in `history/` (one file per quarter).
Status: `todo` | `in-progress` | `done` | `skipped`

## Open items

| # | Status | Item |
|---|--------|------|
| 6.10 | `in-progress` | **HA control plane conversion (2026-07-03)** — DONE: SQLite backed up off-node (integrity-checked); amley00 migrated to embedded etcd (`cluster-init: true`); amley01+02 drained, rejoined as servers (`disable: [servicelb, traefik]`), Longhorn rebuilt between nodes; docs PR #88 open. Docs PR #88 merged + synced. End-to-end verified: 3 etcd members healthy, API answers on all three server IPs, Longhorn 7/7 healthy, apps 16/16 Synced/Healthy, memory 10–24% on servers. First scheduled etcd snapshot VERIFIED (12:00 2026-07-03, one per server, ~24MB, `kubectl get etcdsnapshotfiles`). kube-vip API VIP `10.0.10.250` live (PR #89), kubeconfig switched. Remaining: watch etcd stability for a few days (fsync latency vs Longhorn IO — apiserver restarts / leader elections in `journalctl -u k3s`; since 11.1 the k3s journal is also searchable in OpenObserve, `k3s` stream), then archive to history. Follow-ups deferred below (offsite snapshot copy, stable API endpoint). |
| 6.9 | `in-progress` | Longhorn backups — **first scheduled nightly VERIFIED 2026-07-03**: all 7 volumes backed up at 02:00 America/New_York, all Completed. **Snapshot bloat root-caused 2026-07-05**: 105 pre-2026 local snapshots (22.6Gi — orphaned `system-backup-*`/`c-*` from Oct–Nov 2025) + 7 `preupgrade-*` (23.3Gi) never pruned, because `snapshot-cleanup` only purges *system-generated* snapshots and trim can't free snapshot-pinned blocks; prometheus volume at 45.7Gi actual vs 20Gi nominal. Added `snapshot-delete` recurring job (PR #151); first manual run 2026-07-05 pruned attached volumes to ≤3 snaps BUT freed no space: retain 3 preserves the stale snapshots forever — steady state has only 2 recurring snaps (latest daily + latest weekly backup snap), so nothing ever pushes out the 3rd slot, and deleted snapshots coalesce their blocks into surviving neighbors (prometheus volume still ~46Gi actual). Retain lowered to 2. Remaining: after next 04:30 UTC run verify prometheus actualSize ~13Gi and 2 snaps/volume; `pvc-af0b20e5` (linkding-dev sqlite rollback PVC) is detached so recurring jobs skip it — 44 stale snaps (0.4Gi), delete the PVC when the CNPG rollback window closes; delete the `preupgrade-*` manual backups **in the backupstore** once nightlies have a few days of history (manual backups are never auto-pruned; check ~2026-07-07) and the stale Nov-2025 `SystemBackup` CR `c-ec9t8a-*`; schedule an annual restore drill. |
| 8.2 | `in-progress` | **HA: cheap replicas (~200Mi actual total)** — PR #107 merged + verified 2026-07-04: all four pairs on distinct nodes; alertmanager gossip cluster formed, dedup **verified on a settled cluster** (single page for the second test alert; the first test double-paged, but it ran 90s after both pods restarted with posts 45s apart — startup artifact, not a defect); A/B benchmark re-run DONE (0 errors at full 100 RPS, see benchmarks/results.yaml run 2). **Only remaining: off-LAN tailscale check** (route reachable from cellular — both routers advertise 10.0.10.0/24), then archive. **(c) AMENDED 2026-07-04: homepage reverted to 1 replica** — multi-replica homepage flicker-re-renders on every visit (per-pod in-memory state + round-robin background API calls; known upstream, gethomepage #2305); sticky sessions can't fix it through the 2-replica cloudflared tunnel path. Kept the 256Mi request + 500m limit; homepage joins grafana/linkding on the replicate-by-design-never list. Original audit item follows. — (a) cert-manager webhook ×2: `webhook.replicaCount: 2` + PDB minAvailable 1 + hostname TSC (pod labels `app.kubernetes.io/name: webhook`, `app.kubernetes.io/instance: cert-manager`) — webhook is `failurePolicy=Fail` on all cert-manager CRDs, so while down every production ArgoCD sync fails (each overlay applies a Certificate); ~13Mi. (b) tailscale subnet router ×2: `connector.replicas: 2` in values + `replicas`/`proxyClass` in `templates/connector.yaml` + new `templates/proxyclass.yaml` (ProxyClass `node-spread`, `spec.statefulSet.pod.topologySpreadConstraints`, labels `tailscale.com/managed: "true"` + `tailscale.com/parent-resource: <hostname>`) — CRD `spec.replicas` verified in-cluster (kb/1115); as an STS on a dead node it's stranded indefinitely and you're locked out remotely; ~30Mi. (c) homepage ×2: `apps/homepage/base/deployment.yaml` replicas 2 + TSC (`app: homepage`) and right-size memory request 512Mi→256Mi (observed 112Mi) so the second replica is net-free; readinessProbe already correct. (d) alertmanager ×2: `alertmanagerSpec.replicas: 2` + `podAntiAffinity: "hard"` (~line 92 monitoring values) — gossip clustering is operator-wired, alert-delivery SPOF gone (~30Mi + empty 10Gi PVC); verify no duplicate Pushover pages after rollout. |
| 8.3 | `todo` | **HA: prometheus ×2 (the ~3Gi decision)** — single replica on amley02 (hottest node, 2.8Gi actual); while down NO alert rules evaluate (8.2's AM HA is moot without it). `prometheusSpec.replicas: 2` + `podAntiAffinity: "hard"` (chart default soft = can co-locate) + explicit `resources.requests {cpu: 150m, memory: 3Gi}` (container currently has NO requests). **Fix storage drift first**: values say 50Gi, live PVC is 20Gi (bumped post-creation, never expanded) — align values to 20Gi (TSDB uses 10.5Gi @ 10d retention) or replica-1 gets an asymmetric 50Gi PVC. Cost: ~2.8Gi mem + 20Gi PVC ×3 Longhorn replicas ≈ 60Gi raw (headroom fine: amleyg00 27Gi mem free, Longhorn 140-765GiB/node). Alert dedup is automatic (operator drops `prometheus_replica` label — verified in live config secret); grafana round-robins replicas, post-failover gaps acceptable, Thanos not worth it. Note: replica-0 stays on amley02 — consider nudging monitoring toward amleyg00 (etcd fsync contention, see 6.10). |
| 8.4 | `todo` | **HA: coredns ×2 via GitOps ownership migration (worst SPOF: node loss = all DNS dead ~5-6 min)** — k3s ships coredns as a plain-manifest AddOn (1 replica, no PDB, kubectl edits reverted; HelmChartConfig does NOT apply — verified). Plan: new `infrastructure-helm/coredns/` umbrella chart (coredns/coredns 1.46.0) with replicaCount 2, `service.clusterIP: 10.43.0.10` (the only contract), `k8sAppLabelOverride: "kube-dns"`, priorityClassName system-cluster-critical, CriticalAddonsOnly+control-plane tolerations, PDB maxUnavailable 1, hostname TSC, Corefile reproducing live k3s config. **Three verified traps**: (1) pin `image.tag: "1.14.4"` — chart appVersion defaults would silently DOWNGRADE to 1.13.1; (2) set `prometheus.service.enabled: true` AND `prometheus.monitor.enabled: true`, and flip `coreDns.enabled: false` in monitoring values — else scraping silently disappears; (3) add a static `hosts` block with node IPs (10.0.10.20/21/22, 10.0.10.30) — k3s NodeHosts maintenance stops. Rollout ordering (maintenance window): merge chart (Service stays pending — clusterIP held by kube-dns), then add `coredns` to `disable:` in `/etc/rancher/k3s/config.yaml` per server + rolling `systemctl restart k3s`, manual ArgoCD sync immediately (DNS gap = seconds). Update docs/setup.md disable list both places. Alt fallback (imperative, not in git): `kubectl scale -n kube-system deploy/coredns --replicas=2` persists across restarts since the manifest omits `spec.replicas`, and the shipped TSC spreads the pods. |
| 10.1 | `in-progress` | **kromgo live cluster badges (2026-07-05)** — new `apps/kromgo/` (ghcr.io/home-operations/kromgo 0.15.0) exposes whitelisted Prometheus queries as public shields badges at kromgo.simonjung.io (nodes/pods/cpu/memory/alerts); README embeds them via shields endpoint badges. Config verified end-to-end pre-merge (local docker run of the exact image+config against port-forwarded prod Prometheus — all 5 badges render with correct values/colors). **Remaining manual prerequisites before/at merge**: create Cloudflare tunnel `kromgo` + DNS route `kromgo.simonjung.io`, store credentials.json in 1Password item `Cloudflared - Kromgo` (same pattern as the homepage/linkding tunnels); after merge verify ArgoCD sync + badges render on GitHub, then archive. |
| 10.4 | `in-progress` | **Kyverno audit mode (2026-07-05)** — new `infrastructure-helm/kyverno/` umbrella (kyverno + kyverno-policies charts, both 3.8.1 / v1.18.1): PSS **baseline** set + 2 custom policies (disallow-latest-tag, require-requests — the 8.3 prometheus no-requests finding generalized), **everything Audit** — violations land in PolicyReports, nothing ever blocks admission. Zero-blast-radius guarantees, both belts: `features.forceFailurePolicyIgnore.enabled: true` (main chart) + `failurePolicy: Ignore` (policies chart, default is Fail!) — a down Kyverno can never wedge ArgoCD syncs the way a down cert-manager webhook can (see 8.2a). cleanupController disabled (unused, 2 fewer pods); background scans on so *existing* resources get reported, not just new admissions. Render verified pre-merge: 13 ClusterPolicies, 3 deployments, kubeconform clean (clusterpolicy_v1 in datree catalog). **Remaining after merge**: verify sync + `kubectl get policyreports -A` populates (background scan takes a few min); read the violation inventory for a week, fix own apps app-by-app (plex is the only app with any securityContext today), add `exclude` blocks for by-design violators (longhorn is legitimately privileged); only then consider per-policy Enforce (start with policies that can only hit own manifests). Next rung: policy-reporter UI chart if the raw CRs get tedious. |
| 11.2 | `in-progress` | **GlitchTip error tracking (2026-07-05)** — new `apps-helm/glitchtip/` umbrella, the first tenant of the apps-helm appsets (namespace `glitchtip-production`): chart 9.0.0 / app 6.2.0, Sentry-SDK-compatible — the in-cluster Django apps point stock `sentry-sdk` at an in-cluster DSN, and a later move to sentry.io is just a DSN swap. Web ×2 (chart defaults: PDB minAvailable 1, soft hostname anti-affinity, worker embedded in the web pod) + bundled valkey (official valkey chart, transient cache, no persistence) + own CNPG cluster `glitchtip-db` (2 instances, `longhorn-1replica` 5Gi, same shape as linkding-db 9.1; the chart's `postgresql.enabled` renders a bare Cluster — no resources/PodMonitor — so it stays disabled; no Pooler, event volume is a trickle). DATABASE_URL comes from the CNPG `-app` secret `uri` key; DB migrations run as a helm post-install/pre-upgrade hook Job (ArgoCD runs it as sync hooks, delete-policy `before-hook-creation`); ServiceMonitor on. UI at glitchtip.cluster.simonjung.io (LAN-only, cloudflare-clusterissuer cert; no tunnel — nothing off-LAN reports errors). Render verified pre-merge: 17 resources, kubeconform clean with exact CI flags. 1Password item `glitchtip-secret-key` created (field `SECRET_KEY`), PR #169 merged 2026-07-05. **First-install deadlock found live**: the chart's migrate Job is a helm post-install/pre-upgrade hook → ArgoCD PreSync → on first install it waits on secrets the Sync phase hasn't applied yet (CNPG `-app`, 1Password, valkey) — pod stuck CreateContainerConfigError, namespace empty (would also break the root-app empty-cluster bootstrap). Fix: `migrationJob.enabled: false` + own `templates/migrate-job.yaml` as a **Sync-phase** hook (applied alongside its dependencies; kubelet retries env until the operators publish the secrets); image tag pinned in values so web + migrate never drift — bump `glitchtip.image.tag` with chart bumps (Renovate's regex manager only scans `apps/**`; extend it to apps-helm values as a follow-up). Fix + homepage tile merged (PR #171) and **verified live 2026-07-05**: Synced/Healthy, migrate Job completed in 62s on its 2nd pod (1st errored while CNPG bootstrapped — exactly the intended retry), CNPG 2/2, both web pods up, UI answering, first account registered. **Ops learning from the interim**: deleting an Application mid-wedged-sync deadlocks — cascade deletion waits on the Running operation, the operation waits on the hook Job, the Job's `argocd.argoproj.io/hook-finalizer` is only removed when the operation ends; unstick by terminating the operation (patch `status.operationState.phase: Terminating`, = `argocd app terminate-op`). README platform-capabilities bullet + repo-tree and CLAUDE.md apps-helm notes updated. Remaining: create org + project, wire the DSN into django-starter/shaadimubarak, verify a test event lands. Later rungs: EMAIL_URL (console backend today — invites/alert emails go nowhere), in-app webhook alert rules, uptime monitors. |
| 8.5 | `todo` | **HA audit: subtraction/cleanup** — (a) argocd dead weight: `dex.enabled: false` (no SSO configured, admin login only) + `notifications.enabled: false` (zero triggers configured) → ~37Mi + two fewer pods to reschedule in outages. (b) **latent StorageClass bug**: BOTH `local-path` and `longhorn` are annotated default (tie currently resolves to longhorn by creation time; an SC recreation could silently flip new PVCs to node-local unreplicated storage). Fix: delete the orphaned 321-day-old `default/data-my-release-mariadb-0` local-path PVC FIRST (reclaim needs the provisioner alive), then add `local-storage` to the k3s `disable:` list (same rollout as 8.4 — combine the restarts). Skip-list from the audit (verified, don't revisit): argocd application-controller (replicas=sharding not redundancy w/ 1 cluster; force-delete pod runbook if deploy needed mid-outage), grafana/linkding/plex (sqlite, 8.1 failover covers), transmission (single daemon), metallb/kube-vip (already HA), metrics-server/KSM/prometheus-operator/1password-operator/SUC (reconcile-pause only, nobody notices). |

## Follow-ups / deferred

- **linkding stays 1 replica (both envs, decided 2026-07-05)** — postgres (9.1) already made the data HA; a second render pod buys ~1 min RTO for real machinery. If ever revisited, the honest recipe is shared-nothing: emptyDir data + `secretkey.txt` projected from a Secret (linkding has no SECRET_KEY env; differing keys break sessions/CSRF across pods) + accept per-pod huey queues (`tasks.sqlite3` is an actively-written sqlite WAL — never put it on RWX/NFS). TRAP to re-check first: page snapshots/assets would land on one pod's emptyDir and vanish — fine today (assets/ empty in prod), data-loss if archival gets used. Longhorn RWX rejected: share-manager pod is its own ~1min-failover SPOF + sqlite-on-NFS hazard.
- **OpenObserve follow-ups** (from 11.1, archived 2026-07-05): ~~exclude the log PVC from Longhorn's backups~~ **DONE 2026-07-05**: new `longhorn-ephemeral` StorageClass (1 replica + `recurringJobSelector` stamps the new `no-backup` group at provision — explicit membership replaces the implicit `default` group, so trim/snapshot-delete/snapshot-cleanup still run but the backup jobs skip it; mechanism verified live first: every volume, CNPG included, carries an explicit `recurring-job-group.longhorn.io/default: enabled` label). Migration = one-time delete of the openobserve STS+PVC (volumeClaimTemplates immutable; hours-old disposable logs) — verify the new volume's labels and that the next 02:00 backup skips it. Still open: create a dedicated ingestion user so fluent-bit stops holding root credentials; revisit 20Gi/15d retention after a week of real volume; the "Kubernetes Logs" dashboard lives in OpenObserve's store on the 1-replica PVC, NOT in git — commit its JSON as a re-import artifact if it proves valuable.
- **etcd snapshot offsite copy** (from 6.10) — snapshots land on each server's local disk (00:00/12:00, retention 5); K3s only ships to S3 natively and the NAS is NFS. Options: systemd timer rsyncing `/var/lib/rancher/k3s/server/db/snapshots` to the NAS, or Synology S3-compatible endpoint. Low urgency: cluster state is rebuildable from git.
- ~~Stable API endpoint~~ **DONE 2026-07-03** (PR #89): kube-vip ARP VIP `10.0.10.250:6443` (`infrastructure/kube-vip/`), `tls-san` applied on all three servers, kubeconfig switched to the VIP (backup: `~/.kube/config.bak-pre-vip`). Server *join* configs still name a node IP — fine (only used at join; joined nodes learn all servers).
- **Plex hardware transcode — BLOCKED UPSTREAM on Plex** (diagnosed exhaustively 2026-07-03): Plex's bundled radeonsi driver supports only up to gfx1103 (~2023); Strix Halo is gfx1151. Their transcoder is **musl-linked**, so it cannot load the distro's glibc mesa (verified: system stack encodes h264_vaapi at 10.9× realtime on this node; Plex's own driver fails VAAPI init err 2). Kernel/VCN/permissions/settings/Plex Pass all verified good. Fix must come from Plex shipping a newer driver bundle — watch PMS releases (on 1.43.2, latest stable) and re-test after upgrades (`kubectl exec` grep `gfx[0-9]{3,4}` in the rsv driver; hw works when gfx1151 appears). PRs #90/#91 added mesa/libva to the container — useless across the musl wall; reverted in PR #93 (2026-07-03). Report filed with Plex (draft provided 2026-07-03).

## Healthy (no action)

- All ArgoCD apps Synced/Healthy; fleet on k3s v1.36.2; **all four nodes on Fedora 44 stable** (amley00 reinstalled 2026-07-04, dnf5-automatic Mon 03:00).
- LAN ingress via MetalLB L2 VIP `10.0.10.200` (traefik ×2 replicas); public via Cloudflare Tunnels.
- Longhorn backup target `nfs://192.168.40.118:/volume1/longhorn` available; nightly incrementals scheduled.
- Alerting: Pushover (allowlist routing), cert-expiry rules, k3s false positives silenced.
- Logs: OpenObserve at logs.cluster.simonjung.io — `containers` (all pods, 4/4 nodes) + `k3s` (journal) streams, 15d retention; pipeline self-noise silenced (fluent-bit + server both log at warn).
