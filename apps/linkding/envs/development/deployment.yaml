---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: linkding
spec:
  template:
    spec:
      # Pinned to a MiniPC for benchmark comparability: record 6 in
      # benchmarks/results.yaml measured the knee on amleyg00 (Zen);
      # this measures the same 250m limit on N97 hardware, which is
      # also where production actually runs. Remove when done comparing.
      nodeSelector:
        kubernetes.io/hostname: amley01
