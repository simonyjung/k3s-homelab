---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
    name: cloudbeaver-ingress-certificate
spec:
    secretName: cloudbeaver-ingress-secret
    issuerRef:
        name: cloudflare-clusterissuer
        kind: ClusterIssuer
    dnsNames:
        - cloudbeaver.cluster.simonjung.io
